% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{boot_stat}
\alias{boot_stat}
\title{Bootstrap statistic}
\usage{
boot_stat(x, stat, B = 999L, type = c("ordinary", "balanced"), ...)
}
\arguments{
\item{x}{A numeric vector (or object) containing the data.}

\item{stat}{A function \code{stat(x, ...)} returning a numeric scalar.}

\item{B}{Integer, number of bootstrap resamples.}

\item{type}{Character, one of \code{"ordinary"} or \code{"balanced"}.}

\item{...}{Additional arguments passed to \code{stat}.}
}
\value{
A list with elements
\describe{
\item{t0}{Observed statistic on the original data.}
\item{tstar}{Numeric vector of length \code{B} of bootstrap replicates.}
}
}
\description{
Generate bootstrap replicates of a statistic \eqn{T = t(X_1,\dots,X_n)}.
Ordinary bootstrap draws i.i.d. samples from the empirical distribution
\eqn{\hat F_n}; the balanced bootstrap ensures each index appears exactly
\code{B} times across all resamples.
}
\examples{
x <- rnorm(50)
b <- boot_stat(x, stat = mean, B = 200)
boot_se(b)
boot_ci(b, method = "percentile")
}
